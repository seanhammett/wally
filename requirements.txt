# Python 3.9+ (3.12 recommended). Install with:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# or, with uv:
#   uv venv && uv pip install -r requirements.txt
geopandas>=1.0
pandas>=2.0
shapely>=2.0
pyyaml>=6.0
rasterio>=1.3
